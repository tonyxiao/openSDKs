{
  "name": "@opensdks/sdk-qbo",
  "version": "0.0.1",
  "main": "dist/index.js",
  "module": "index.ts",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "**/*.ts",
    "!**/*.spec.ts",
    "!**/*.json"
  ],
  "scripts": {
    "build": "concurrently npm:build:*",
    "build:dts": "mkdir -p dist && cp *.d.ts ./dist",
    "build:json": "mkdir -p dist && npx tsx ../../bin/oasJsonToJs.ts ./ ./dist",
    "build:ts": "tsc -p ./tsconfig.json",
    "clean": "rm -rf ./dist",
    "generate": "run-s generate:*",
    "generate:json": "npx tsx ./qbo.openapi.ts | prettier --parser json > ./qbo.oas.json",
    "generate:types": "openapi-typescript ./qbo.oas.json --output qbo.oas.d.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@opensdks/runtime": "workspace:*",
    "@opensdks/util-zod": "workspace:*",
    "concurrently": "^8.2.2",
    "openapi-typescript": "6.7.1",
    "prettier": "3.1.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "type": "module"
}
