{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    // This makes React implicitly available in mdx files
    "jsx": "react-jsx",
    "paths": {} // Do not remap any workspace packages otherwise messes with eslint setup
  },
  "include": ["**/*.ts", "**/*.mts", "**/*.tsx"]
}
